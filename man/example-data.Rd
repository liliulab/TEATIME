% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\name{example-data}
\alias{example-data}
\title{Example data bundled with TEATIME}
\description{
Three files are provided in \code{inst/extdata} for testing and validation.
Access them with \code{\link[=system.file]{system.file()}}:
}
\details{
\describe{
\item{\code{exampledata.rds}}{Raw somatic mutation data (REF/ALT counts) used
as the primary test input.}
\item{\code{MAGOS.rds}}{Pre-computed MAGOS clustering result derived from
\code{exampledata.rds}. Use for reproducible validation runs without
re-running MAGOS.}
\item{\code{TEATIME.final.txt}}{Reference output from the original TEATIME
pipeline on the same data. Used to verify v2 results match within
tolerance.}
}
}
\examples{
magos <- readRDS(system.file("extdata", "MAGOS.rds", package = "TEATIME"))
input <- list(purity = magos$purity, result = magos$results)
result <- TEATIME.run(input, beta = 0.9, depth = round(mean(magos$results$depth.1)),
                      seed = 123, write_final = FALSE)
print(result)

}
