% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline.R
\name{TEATIME.run}
\alias{TEATIME.run}
\title{Run the TEATIME v2 pipeline}
\usage{
TEATIME.run(
  input,
  beta = 0.9,
  depth = NA,
  p_thre = 0.01,
  input_format = "magos",
  growth_model = "exponential",
  verbose = FALSE,
  output_folder = "./",
  output_prefix = "TEATIME",
  id = "T01",
  write_final = TRUE,
  seed = NA,
  extra = list()
)
}
\arguments{
\item{input}{Input data. Either a list with elements \code{purity} and \code{result}
(MAGOS object) when \code{input_format = "magos"}, a \code{data.frame} with columns
\code{REF} and \code{ALT} (when \code{input_format = "vcf"}), or a \code{data.frame} already
containing \code{vaf.1}, \code{depth.1}, and \code{colors} (when \code{input_format = "raw"}).}

\item{beta}{Numeric. Survival rate (default \code{0.9}). Used as the \code{r}
parameter of the growth model.}

\item{depth}{Integer or \code{NA}. Mean sequencing depth. If \code{NA} (default),
computed from the data.}

\item{p_thre}{Numeric. P-value threshold for breakpoint and fitness tests
(default \code{0.01}).}

\item{input_format}{Character. One of \code{"magos"} (default), \code{"vcf"}, or
\code{"raw"}.}

\item{growth_model}{Character. Name of a registered growth model (default
\code{"exponential"}). Register custom models with \code{\link[=register_growth_model]{register_growth_model()}}.}

\item{verbose}{Logical. If \code{TRUE}, prints step-level timing messages
(default \code{FALSE}).}

\item{output_folder}{Character. Directory for output files (default \code{"./"}).}

\item{output_prefix}{Character. Prefix for output file names (default
\code{"TEATIME"}).}

\item{id}{Character. Sample identifier written to the result table (default
\code{"T01"}).}

\item{write_final}{Logical. If \code{TRUE} (default), writes
\verb{<output_prefix>.final.txt} to \code{output_folder}.}

\item{seed}{Integer or \code{NA}. Random seed for reproducibility (default \code{NA}).}

\item{extra}{Named list of additional parameters passed to custom growth
models via \code{ctx$extra} (default \code{list()}). Ignored by the built-in
exponential model.}
}
\value{
A one-row \code{data.frame} with columns:
\describe{
\item{name}{Sample ID (\code{id} argument).}
\item{mu}{Estimated mutation rate (mutations per cell division).}
\item{s}{Estimated selection coefficient.}
\item{emergence_time}{Estimated emergence time of the subclone (cell
divisions).}
\item{tau}{Subclone expansion score (tend / emergence_time).}
\item{p}{Estimated clonal fraction.}
}
All numeric columns are \code{NA} when the pipeline cannot produce a reliable
estimate.
}
\description{
Main entry point for TEATIME. Accepts pre-processed MAGOS clustering output
or raw VCF-like data, estimates evolutionary parameters, and optionally
writes a tab-separated result file.
}
\examples{
\dontrun{
# Using a pre-computed MAGOS object
input <- list(purity = magos.114$purity, result = magos.114$results)
result <- TEATIME.run(input, beta = 0.9, depth = 150, seed = 42)
}
}
